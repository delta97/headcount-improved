"""Builders that project the canonical tree into packages for runtimes other than Claude Code.

Nothing here is a second source of truth: every builder reads config/departments.json and the
plugin tree, and writes only derived output.
"""
